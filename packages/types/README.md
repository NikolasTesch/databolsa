# @databolsa/types

Tipos e contratos compartilhados da API (DTOs de request/response, enums de
domínio: `asset_class`, `currency`, `data_source`, `transaction.type`).

Consumido por `apps/api` (origem da verdade do contrato) e pelos clientes
`apps/web` e `apps/mobile` para tipagem ponta a ponta.
